/*jshint esnext:true */
/** @preserve
* hello
*/

/*global $, k1, p, $p */

var square = (x) => {
  return x * x;
};

var
  x= y + z;
alert(square(2)  +4);
for (var element of [1, 2, 3]) {
  res.push(element * element);
}    
var t = 3 * 5; 
var k1 = 5;
 // #put RADTODEG(3)+";"
var $p =5;
var [a, b] = ['hello', 'world'];